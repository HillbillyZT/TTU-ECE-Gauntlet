extends Button


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	"""var roster = get_node("/root/Player/Player.gd").roster
	print(roster)
	if roster != null:
		self.text = roster[0]
	else:
		self.text = "No Prof"
	"""

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
